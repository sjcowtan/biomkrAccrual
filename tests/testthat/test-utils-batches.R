# Testing the code for running multiple simulations

biomkrAccrualSim(
  n = 50,
  shared_control = TRUE,
  target_times = c(2, 4),
  fixed_centre_starts = TRUE,
  quietly = TRUE,
  keep_files = TRUE
)

